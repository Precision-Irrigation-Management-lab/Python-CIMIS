# Documentation build requirements for ReadTheDocs
sphinx>=5.0.0,<9.0.0
sphinx-rtd-theme>=2.0.0
requests>=2.25.0
myst-parser>=2.0.0  # For Markdown support if needed
sphinx.ext.napoleon  # Built into Sphinx, no separate package needed
